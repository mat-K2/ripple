!!!
%html

  %head
    %title
      Ripple
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar.navbar-inverse.navbar-fixed-top
      .navbar-inner
        .container
          = link_to "Ripple", root_path, :class => "brand"
          .nav-collapse.collapse
            - if user_signed_in?
              .navbar-text.pull-right
                %ul.nav
                  %li
                    = link_to "開発者向け", make_services_path
                  %li.dropdown
                    = link_to "#{current_user.name} <b class='caret'></b>".html_safe, "#", { :class => "dropdown-toggle", "data-toggle" => "dropdown" }
                    %ul{ :class => "dropdown-menu", :role => "menu", "aria-labelledby" => "dLabel" }
                      %li.presentation
                        = link_to "ログアウト", destroy_user_session_path, :method => "delete"

            - else
              .pull-right
                %ul.nav
                  %li
                    = link_to "ログイン", new_user_session_path, :class => "pull-right" unless user_signed_in?
    .container
      = yield
